# Build artifacts
engine/omaseal
engine/engine
*.exe

# Go
*.test
coverage.out
vendor/

# Plugin package artifacts
dist/
*.tar.gz
*.zip

# Local testing
local/

# Agent access guide — kept locally, never shipped in plugin tree
/AGENTS.md

# Agent MCP configs (per-repo omaseal wiring)
/.claude/mcp.json
/.codex/mcp.json
/.cursor/mcp.json
/.devin/mcp.json
/.agy/mcp.json
/.hermes/mcp.json
